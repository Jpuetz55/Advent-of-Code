fn main() {
    // Declare String with Calibration parameters
    // declare total to add calabration parameters to

    // Split raw calibration parameters by the new line and put them in a vector

    // Loop over each item in array
        // ------ loop operations --------------------------------

        

        // loop through each item
            // loop through characters in array item.
                // declare temporary variable to hold the firstNumber and the lastNumber and set them to NULL
                // if character == number && firstNumber == NULL then save to firstNumber and continue to next char 
                // if character == number && firstNumber !== NULL then save to lastNumber and continue 
            //end loop -- when loop ends firstNumber should contain the first number and lastCharacter should contain the last number
                
            // 
            //declare variable to hold the final parameter calculation 
            //algo to convert the first and last number values into a number. i.e. 1 and 2 == 12
                //(first number * 10) + finalNumber?
            // add result of the calculation to the total.
        //end loop
        // function returns the value of total 


    println!("Hello, world!");
}
